---
import ActivityCard from './Card.astro';
---

<section id="activities">
    <h2><span class="inverted">20 years</span> of experience building low-carbon, high speed, highly crafted websites</h2>
    <ul class="content-center">
      <ActivityCard icon="lucide:network">
        <Fragment slot="title">
          Technical leadership
        </Fragment>
        <Fragment slot="content">
          At the service of agencies as well as my own projects. My work is documented and transparent.
        </Fragment>
      </ActivityCard>
      <ActivityCard icon="lucide:earth">
        <Fragment slot="title">
          Eco-designed Websites
        </Fragment>
        <Fragment slot="content">
          Also aesthetic, accessible, high-performing and efficient.
        </Fragment>
      </ActivityCard>
      <ActivityCard  icon="lucide:heart">
        <Fragment slot="title">
          User Experiences
        </Fragment>
        <Fragment slot="content">
          Online or on physical kiosks.
        </Fragment>
      </ActivityCard>
      <ActivityCard  icon="lucide:slack">
        <Fragment slot="title">
          Project Management
        </Fragment>
        <Fragment slot="content">
          To support both agencies and brands in their digital projects.
        </Fragment>
      </ActivityCard>
    </ul>
</section>
<style lang="scss">
section {
  $sectionGap: 4rem;
  
  position: relative;
  margin: 8rem auto;
  max-width: 1920px;
  padding: 0;
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  grid-gap: 4rem;

  >:last-child {
    justify-self: flex-end;
  }
  h2 {
    // place-content: center;
    @include above(calc(600px + ($sectionGap + ($layoutMargin * 2)))) {
      height: fit-content;
      position: sticky;
      top: 60px;
      padding-top: 1rem;
    }
  }
  ul {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: var(--layoutMargin);
    margin: 0 auto;
    padding: 0;
    list-style: none;
  }
}
</style>